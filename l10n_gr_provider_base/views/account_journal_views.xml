<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_journal_form_l10n_gr_prov" model="ir.ui.view">
        <field name="name">account.journal.form.l10n.gr.prov</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <field name="invoice_reference_model" position="after">
                <field name="l10n_gr_edi_inv_type_default"
                       invisible="type not in ('sale', 'purchase')"
                       string="Προεπιλογή Τύπου myDATA"/>
                <field name="l10n_gr_prov_delivery_note" invisible="type != 'sale'"/>
            </field>
        </field>
    </record>

</odoo>
